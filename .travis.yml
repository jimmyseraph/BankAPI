language: java
sudo: false

cache:
  directories:
  - "$HOME/.m2"

jobs:
  include:
  - stage: test
    jdk: oraclejdk8
    script: mvn test
  - stage: build
    jdk: oraclejdk8
    script: mvn build -DskipTests=true

branches:
  only:
  - master